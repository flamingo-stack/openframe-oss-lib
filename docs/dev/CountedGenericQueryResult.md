# CountedGenericQueryResult Documentation

## Overview
The `CountedGenericQueryResult` class extends `GenericQueryResult` to include a count of filtered items, providing additional context for query results.

## Core Components
- **filteredCount**: The number of items that match the filter criteria.
