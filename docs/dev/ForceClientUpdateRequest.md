# Force Client Update Request Documentation

## Overview
The `ForceClientUpdateRequest` class represents a request to update clients based on a list of machine IDs.

## Core Components
- **Machine IDs**: A list of machine identifiers that need to be updated.

## Usage
This class is used in client management operations to facilitate updates across multiple machines.