# GoogleClientRegistrationStrategy Documentation

## Overview
The `GoogleClientRegistrationStrategy` class handles the registration strategy for Google clients, extending the base OIDC client registration strategy.

## Properties
- **googleProps**: Configuration properties for Google SSO.

## Methods
- `providerId()`: Returns the provider ID for Google.
- `props()`: Returns the properties for Google SSO.