# ReactiveIntegratedToolRepository Documentation

## Overview
The `ReactiveIntegratedToolRepository` interface extends the `ReactiveMongoRepository` and provides methods for managing integrated tools in a reactive manner.

## Methods
- **findByType(String type)**: Finds an integrated tool by its type.