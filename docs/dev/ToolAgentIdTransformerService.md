# ToolAgentIdTransformerService Documentation

## Overview
The `ToolAgentIdTransformerService` class transforms agent tool IDs based on the tool type using a list of transformers.

## Key Methods
- **transform(ToolType toolType, String agentToolId, boolean lastAttempt)**: Applies the appropriate transformation based on the tool type.