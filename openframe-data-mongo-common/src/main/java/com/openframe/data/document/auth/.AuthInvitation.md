<!-- source-hash: 4f8b778caf8b273d97a856190a85b0df -->
MongoDB document entity representing an authentication invitation scoped to a tenant, combining tenant-based indexing with user invitation data.

## Key Components

- **`AuthInvitation`** — Extends `Invitation` and `TenantScoped`, inheriting invitation fields alongside tenant context
- **Compound Index** — `{'tenantId': 1, 'email': 1}` ensures efficient tenant-scoped lookups by email
- **Lombok Annotations** — `@Data`, `@SuperBuilder`, `@NoArgsConstructor`, and `@EqualsAndHashCode(callSuper = true)` handle boilerplate generation while preserving parent class equality logic

## Usage Example

```java
AuthInvitation invite = AuthInvitation.builder()
    .tenantId("tenant-123")
    .email("user@example.com")
    .build();

authInvitationRepository.save(invite);

// Lookup by tenant + email (uses compound index)
Optional<AuthInvitation> found = authInvitationRepository
    .findByTenantIdAndEmail("tenant-123", "user@example.com");
```

> **Note:** The compound index on `tenantId` + `email` enforces fast scoped queries and should align with your repository query patterns to avoid full collection scans.