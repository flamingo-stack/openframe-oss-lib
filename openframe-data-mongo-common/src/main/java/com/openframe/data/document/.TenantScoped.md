<!-- source-hash: 62196cc1180a5299018ee6edf33ce96d -->
Marker interface that enforces tenant isolation on document entities by requiring implementations to expose a `tenantId` field.

## Key Components

| Member | Type | Description |
|--------|------|-------------|
| `getTenantId()` | `String` | Returns the tenant identifier associated with the document |
| `setTenantId(String tenantId)` | `void` | Assigns a tenant identifier to the document |

## Usage Example

```java
@Document
public class TicketDocument implements TenantScoped {

    @Id
    private String id;

    private String tenantId;

    @Override
    public String getTenantId() {
        return tenantId;
    }

    @Override
    public void setTenantId(String tenantId) {
        this.tenantId = tenantId;
    }
}
```

Implementing `TenantScoped` allows repository layers or interceptors to automatically filter queries by `tenantId`, ensuring strict data isolation between MSP tenants in the OpenFrame platform.

**Source:** [`TenantScoped.java`](https://github.com/flamingo-stack/openframe-oss-lib/blob/main/src/main/java/com/openframe/data/document/TenantScoped.java)