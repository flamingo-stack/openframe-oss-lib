<!-- source-hash: da6cbc740dc9dc7a8514675e67d30067 -->
MongoDB document entity representing an OAuth 2.0 client registration, scoped to a specific tenant within the OpenFrame multi-tenant architecture.

## Key Components

| Member | Type | Description |
|---|---|---|
| `id` | `String` | MongoDB document identifier (`@Id`) |
| `tenantId` | `String` | Indexed tenant scope identifier (implements `TenantScoped`) |
| `clientId` | `String` | OAuth client identifier |
| `clientSecret` | `String` | OAuth client secret |
| `machineId` | `String` | Associated machine/service identifier |
| `redirectUris` | `String[]` | Allowed redirect URIs for authorization flows |
| `grantTypes` | `String[]` | Permitted OAuth grant types |
| `scopes` | `String[]` | Permitted OAuth scopes |
| `roles` | `String[]` | Assigned roles (defaults to empty array) |
| `enabled` | `boolean` | Client active status (defaults to `true`) |

**Supported grant types:** `authorization_code`, `password`, `client_credentials`, `refresh_token`

## Usage Example

```java
OAuthClient client = new OAuthClient();
client.setTenantId("tenant-abc");
client.setClientId("my-service-client");
client.setClientSecret("s3cr3t");
client.setMachineId("machine-001");
client.setRedirectUris(new String[]{"https://app.example.com/callback"});
client.setGrantTypes(new String[]{"authorization_code", "refresh_token"});
client.setScopes(new String[]{"read", "write"});
client.setRoles(new String[]{"ROLE_API_USER"});
client.setEnabled(true);

// Persisted to MongoDB collection: oauth_clients
mongoTemplate.save(client);
```

## Notes

- Stored in the `oauth_clients` MongoDB collection
- `tenantId` is indexed for efficient multi-tenant queries
- Implements `TenantScoped` to enforce tenant isolation across the platform
- Source: [`OAuthClient.java`](https://github.com/flamingo-stack/openframe-oss-lib/blob/main/src/main/java/com/openframe/data/document/oauth/OAuthClient.java)