<!-- source-hash: 7d5f6c5fc9513295972b5295aee6a6f5 -->
MongoDB document entity that tracks usage statistics for API keys, including request counts and last-used timestamps, scoped to a specific tenant.

## Key Components

| Component | Description |
|---|---|
| `ApiKeyStats` | Main document class mapped to the `api_key_stats` MongoDB collection |
| `TenantScoped` | Implemented interface enforcing multi-tenant data isolation via `tenantId` |
| `@Document` | Spring Data MongoDB annotation binding the class to the `api_key_stats` collection |
| `@Indexed` | MongoDB index on `tenantId` for efficient tenant-scoped queries |

### Fields

| Field | Type | Description |
|---|---|---|
| `id` | `String` | MongoDB document identifier (`@Id`) |
| `tenantId` | `String` | Tenant identifier (indexed) |
| `totalRequests` | `Long` | Cumulative request count |
| `successfulRequests` | `Long` | Count of successful requests |
| `failedRequests` | `Long` | Count of failed requests |
| `lastUsed` | `LocalDateTime` | Timestamp of the most recent API key usage |

## Usage Example

```java
// Build and persist a new stats document
ApiKeyStats stats = ApiKeyStats.builder()
    .tenantId("tenant-abc")
    .totalRequests(1000L)
    .successfulRequests(980L)
    .failedRequests(20L)
    .lastUsed(LocalDateTime.now())
    .build();

apiKeyStatsRepository.save(stats);

// Fetch stats for a specific tenant
List<ApiKeyStats> tenantStats = apiKeyStatsRepository.findByTenantId("tenant-abc");
```

## Source

[`ApiKeyStats.java`](https://github.com/flamingo-stack/openframe-oss-lib/blob/main/src/main/java/com/openframe/data/document/apikey/ApiKeyStats.java)