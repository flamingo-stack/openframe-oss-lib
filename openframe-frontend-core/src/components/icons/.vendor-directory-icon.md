<!-- source-hash: 9f5009b34317d5970b3bb61aa96ef597 -->
A deprecated SVG icon component representing a vendor directory with a search/magnifier overlay, used in the Flamingo MSP platform UI.

## Key Components

**`VendorDirectoryIconProps`** — Interface defining optional props:
- `color` — Fill color for SVG paths (default: `'white'`)
- `className` — CSS class string (default: `""`)
- `width` — Icon width in pixels (default: `20`)
- `height` — Icon height in pixels (default: `20`)

**`VendorDirectoryIcon`** — Deprecated React functional component rendering a two-path SVG: a package/box shape with a search magnifier overlay, fixed to a `20x20` viewBox.

> **⚠️ Deprecated:** Use icons from `icons-v2-generated` instead.

## Usage Example

```typescript
import { VendorDirectoryIcon } from './vendor-directory-icon';

// Default usage
<VendorDirectoryIcon />

// Custom size and color
<VendorDirectoryIcon
  width={24}
  height={24}
  color="#FFC008"
  className="mr-2"
/>
```