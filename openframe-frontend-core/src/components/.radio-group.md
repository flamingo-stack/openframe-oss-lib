<!-- source-hash: aa362cd8fecfbbb02183e4140b3b905a -->
A React radio group component built on Radix UI primitives with custom styling and theming support for the OpenFrame Design System (ODS).

## Key Components

- **RadioGroup** - The main container component that wraps multiple radio options with grid layout
- **RadioGroupItem** - Individual radio button component with custom indicator and accessibility features

Both components are forwardRef-enabled and accept all standard Radix UI RadioGroup props while applying ODS theming classes.

## Usage Example

```typescript
import { RadioGroup, RadioGroupItem } from "./components/ui/radio-group"

function SettingsForm() {
  const [theme, setTheme] = useState("light")
  
  return (
    <form>
      <label htmlFor="theme-selection">Choose theme:</label>
      <RadioGroup 
        value={theme} 
        onValueChange={setTheme}
        className="flex flex-col space-y-2"
      >
        <div className="flex items-center space-x-2">
          <RadioGroupItem value="light" id="light" />
          <label htmlFor="light">Light</label>
        </div>
        <div className="flex items-center space-x-2">
          <RadioGroupItem value="dark" id="dark" />
          <label htmlFor="dark">Dark</label>
        </div>
        <div className="flex items-center space-x-2">
          <RadioGroupItem value="system" id="system" />
          <label htmlFor="system">System</label>
        </div>
      </RadioGroup>
    </form>
  )
}
```

The component includes focus management, keyboard navigation, disabled states, and uses the Circle icon from Lucide React as the checked indicator with ODS accent colors.