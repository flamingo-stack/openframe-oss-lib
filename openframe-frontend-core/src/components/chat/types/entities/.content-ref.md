<!-- source-hash: d15c6ea0015118d3460fb4327e86fe26 -->
Backwards-compatibility re-export stub that preserves existing import paths for `ContentRef` and `ContentRefWithReason` after their canonical definitions were relocated to `src/types/content-ref.ts`.

## Key Components

- **`ContentRef`** — re-exported from `src/types/content-ref.ts`; the canonical content reference type shared across the related-content rail and chat entities.
- **`ContentRefWithReason`** — re-exported from `src/types/content-ref.ts`; extends `ContentRef` with a reason field.

## Usage Example

```typescript
// Existing imports continue to work unchanged — no migration required.
import type { ContentRef, ContentRefWithReason } from './content-ref';

const ref: ContentRef = { id: '123', type: 'article' };

const refWithReason: ContentRefWithReason = {
  ...ref,
  reason: 'Related to current investor update',
};
```

> **Note:** Prefer importing directly from the canonical location for new code:
> ```typescript
> import type { ContentRef } from 'src/types/content-ref';
> ```

## Source

[`content-ref.ts`](https://github.com/flamingo-stack/openframe-oss-lib/blob/main/content-ref.ts)