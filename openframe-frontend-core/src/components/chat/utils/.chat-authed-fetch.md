<!-- source-hash: b57cf7dd5609e4a80ebf96f0658340cc -->
Re-exports `embedAuthedFetch` under the legacy `chatAuthedFetch` alias to maintain backwards compatibility for existing chat-namespace callers.

## Key Components

| Export | Type | Description |
|--------|------|-------------|
| `chatAuthedFetch` | Re-export (alias) | Legacy alias for `embedAuthedFetch` from `utils/embed-authed-fetch` |

## Usage Example

**New code** should import directly from the canonical source:

```typescript
import { embedAuthedFetch } from '@flamingo-stack/openframe-frontend-core/utils'
```

**Legacy callers** (still compiling via this shim):

```typescript
import { chatAuthedFetch } from './chat-authed-fetch'
// chatAuthedFetch === embedAuthedFetch — same function, legacy name
```

> **⚠️ Deprecated:** This file is a compatibility shim only. All new code — whether in the chat or ticket center namespace — should import `embedAuthedFetch` from `@flamingo-stack/openframe-frontend-core/utils` directly. Wire headers (`X-Chat-Act-As`, etc.) are unchanged by the rename.