<!-- source-hash: 585563381a0d7558b71c533935db93f8 -->
A wrapper component that maintains a consistent aspect ratio for its content, built on top of Radix UI's AspectRatio primitive.

## Key Components

- **AspectRatio** - The main component that constrains child content to a specific aspect ratio

## Usage Example

```typescript
import { AspectRatio } from "./aspect-ratio"

// 16:9 video aspect ratio
<AspectRatio ratio={16 / 9} className="bg-muted">
  <img
    src="/placeholder.jpg"
    alt="Photo"
    className="rounded-md object-cover w-full h-full"
  />
</AspectRatio>

// Square aspect ratio for profile images
<AspectRatio ratio={1} className="bg-muted">
  <div className="flex items-center justify-center h-full">
    <span>1:1 Square</span>
  </div>
</AspectRatio>
```

The component accepts a `ratio` prop (width/height) and ensures the container maintains that proportion regardless of screen size, making it ideal for responsive images, videos, or any content requiring consistent dimensions.