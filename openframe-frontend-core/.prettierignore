# Build output and caches
dist
node_modules
storybook-static
.yalc
*.tsbuildinfo
coverage

# Generated by `npm run generate:icons` from the raw SVGs — never hand-edited,
# and reformatting it would produce a diff on every regeneration.
src/components/icons-v2-generated

# Lock files are tool-owned.
package-lock.json

# 1 218 markdown files that no tool has ever formatted. Including them in the
# first pass would bury the code diff; lifted as its own change once the
# code-formatting commit has landed. (2026-08-24)
*.md
*.mdx
