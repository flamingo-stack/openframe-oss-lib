package com.openframe.api.service.ticket;

/** Property keys gating the ticket domain beans. */
public final class TicketFeature {

    public static final String ENABLED = "openframe.features.tickets.enabled";
    public static final String LIFECYCLE_ENABLED = "openframe.features.tickets.lifecycle.enabled";

    private TicketFeature() {
    }
}
