package com.openframe.api.dto.device;

/**
 * Agent log levels, as carried by the {@code level} label of the agent-logs Loki stream.
 */
public enum DeviceLogLevel {
    DEBUG,
    INFO,
    WARN,
    ERROR
}
