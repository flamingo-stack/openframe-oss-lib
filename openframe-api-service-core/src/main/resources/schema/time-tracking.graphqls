extend type Query {
    currentTimer: TimeEntry
    myTimeEntries(period: DateRangeInput, search: String, sort: SortInput, first: Int, after: String): TimeEntryConnection!
    employeeTimeEntries(employeeId: ID!, period: DateRangeInput, search: String, sort: SortInput, first: Int, after: String): TimeEntryConnection!
    employeeTimeStats(employeeId: ID!, period: DateRangeInput!): EmployeeTimeStats!
    timeEntry(id: ID!): TimeEntry
}

extend type Mutation {
    startTimer(input: StartTimerInput): TimeEntry!
    pauseTimer: TimeEntry!
    resumeTimer: TimeEntry!
    stopTimer(input: StopTimerInput): TimeEntry!
    cancelTimer: Boolean!

    createTimeEntry(input: CreateTimeEntryInput!): TimeEntry!
    updateTimeEntry(input: UpdateTimeEntryInput!): TimeEntry!
    deleteTimeEntry(id: ID!): Boolean!
}

type TimeEntry implements Node {
    id: ID!
    userId: ID!
    ticketId: ID
    ticketNumber: Int
    ticketTitle: String
    ticket: Ticket
    notes: String
    startedAt: Instant!
    endedAt: Instant
    pausedAt: Instant
    durationSeconds: Long!
    breakSeconds: Long!
    state: TimerState!
    source: TimeEntrySource!
    createdAt: Instant
    updatedAt: Instant
}

enum TimerState {
    RUNNING
    PAUSED
    COMPLETED
}

enum TimeEntrySource {
    TIMER
    MANUAL
}

type TimeEntryConnection {
    edges: [TimeEntryEdge!]!
    pageInfo: PageInfo!
    filteredCount: Int!
}

type TimeEntryEdge {
    node: TimeEntry!
    cursor: String!
}

type EmployeeTimeStats {
    todayTotalSeconds: Long!
    todayEntryCount: Long!
    periodTotalSeconds: Long!
    periodEntryCount: Long!
    averagePerDaySeconds: Long!
}

input StartTimerInput {
    ticketId: ID
    notes: String
}

input StopTimerInput {
    ticketId: ID
    notes: String
}

input CreateTimeEntryInput {
    userId: ID!
    ticketId: ID
    notes: String
    startedAt: Instant!
    durationSeconds: Long!
}

input UpdateTimeEntryInput {
    id: ID!
    ticketId: ID
    notes: String
    startedAt: Instant
    durationSeconds: Long
}
