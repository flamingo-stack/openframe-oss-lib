services:
  mongo:
    image: mongo:7
    container_name: openframe-test-mongo
    ports:
      - "27027:27017"
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 5s
      timeout: 5s
      retries: 10
