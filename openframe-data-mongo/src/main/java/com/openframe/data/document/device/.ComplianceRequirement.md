<!-- source-hash: 6f6e1784ecdfafa3c51da9a30c91590e -->
A data class representing compliance requirements for device security standards and controls, tracking compliance status and verification details.

## Key Components

- **standard** - The compliance framework identifier (e.g., "NIST", "ISO27001")
- **control** - Specific control or requirement within the standard
- **compliant** - Boolean flag indicating current compliance status
- **details** - Additional information about the compliance state
- **lastCheck** - Timestamp of the most recent compliance verification

## Usage Example

```java
ComplianceRequirement requirement = new ComplianceRequirement();
requirement.setStandard("NIST");
requirement.setControl("AC-2.1");
requirement.setCompliant(true);
requirement.setDetails("Multi-factor authentication enabled for all admin accounts");
requirement.setLastCheck(Instant.now());

// Check compliance status
if (requirement.isCompliant()) {
    System.out.println("Device meets " + requirement.getStandard() + 
                      " requirement for control " + requirement.getControl());
}

// Track when compliance was last verified
Duration timeSinceCheck = Duration.between(requirement.getLastCheck(), Instant.now());
if (timeSinceCheck.toDays() > 30) {
    System.out.println("Compliance check may be outdated");
}
```